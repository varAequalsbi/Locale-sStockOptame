import React from 'react';
import ReactDOM from 'react-dom/client';
import StockOptameApp from './StockOptameApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StockOptameApp />
  </React.StrictMode>
);
